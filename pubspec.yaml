name: objective_db
description: BSON based Database
version: 0.3.2+26
repository: https://github.com/LiveUser/objective_db

environment:
  sdk: ^3.5.1

# Add regular dependencies here.
dependencies:
  power_plant: ^1.0.2+3
  chromarotor: ^1.0.6
  bson: ^5.0.8

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
